services:
  - type: web
    name: phishguard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
